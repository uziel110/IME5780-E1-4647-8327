package elements;

public class Grid {
    // 3DDDA algorithm to improve rendering performance
    private double _minX, _maxX, _minY, _maxY, _minZ, _maxZ;
    /**
     * how mach voxel in width / height / depth
     */
    private int _density;

    public static class Voxel{
        private int _x;
        private int _y;
        private int _z;


    }
}